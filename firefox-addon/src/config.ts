// Native-messaging host name. MUST match /^\w+(\.\w+)*$/ (no hyphens — browser rule),
// and must equal the manifest "name" + the mozeidon_z.json filename.
export const ADDON_NAME = "mozeidon_z"
export const DEBUG = true
